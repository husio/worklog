#!/bin/sh

set -ex

today=`date '+# %_d %b %Y %A'`
logfile=${LOGFILE:-$HOME/dailylog}

if ! grep -s -E "^$today\$" $logfile
then
	echo "\n$today" >> $logfile
fi

vim -u NONE "+normal Gzzo" +startinsert $logfile
